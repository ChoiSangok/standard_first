//[부가서비스]
module.exports = {
    //자동완성 datainfo
    autoData : async(paramSet, searchResult) => {
        result = searchResult.hits.hits;
        let response = [];

        // 서비스 Response
    },
}
